services:
  - type: web
    name: crop-recommender-api
    runtime: python
    buildCommand: "pip install -r src/requirements.txt"
    startCommand: "cd src && gunicorn --bind 0.0.0.0:$PORT app:app"
    plan: free